apiVersion: v1
kind: Service
metadata:
  name: app
  namespace: myapp
spec:
  selector:
    app: app
  ports:
    - port: 80
      targetPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: myapp
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017

---
apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: myapp
spec:
  selector:
    app: loki
  ports:
    - port: 3100
      targetPort: 3100

---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: myapp
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090

---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: myapp
spec:
  selector:
    app: grafana
  ports:
    - port: 3000
      targetPort: 3000

